name: 'Deploy para VPS'

on:
  push:
    branches:
      - main

jobs:
  simple_test:
    runs-on: ubuntu-latest

    steps:
      - name: 'Baixar o código'
        uses: actions/checkout@v4

      - name: 'Criar arquivo no servidor'
        uses: appleboy/ssh-action@v1.1.0
        with:
          host: '164.152.34.246'
          username: 'ubuntu'
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            touch ~/arquivo_de_teste.txt
            ls -l ~/
          debug: true
